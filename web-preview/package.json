{
  "name": "equalpath-iteration1-web",
  "version": "1.0.0",
  "private": true,
  "type": "module",
  "description": "EqualPath Iteration 1 as a local-only web application.",
  "scripts": {
    "dev": "vite",
    "test": "vitest run",
    "build": "vite build",
    "preview": "vite preview",
    "check": "npm test && npm run build"
  },
  "engines": {
    "node": ">=22"
  },
  "dependencies": {
    "react": "^19.2.0",
    "react-dom": "^19.2.0"
  },
  "devDependencies": {
    "@vitejs/plugin-react": "^5.0.4",
    "vite": "^7.1.14",
    "vitest": "^3.2.4"
  }
}
