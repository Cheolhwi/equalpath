{
  "name": "equalpath-iteration1-web-preview",
  "version": "0.1.0",
  "private": true,
  "type": "module",
  "scripts": {
    "test": "node --test tests/*.test.mjs",
    "build": "node scripts/build.mjs",
    "check": "npm test && npm run build"
  },
  "engines": {
    "node": ">=22"
  }
}
