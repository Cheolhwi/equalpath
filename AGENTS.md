# EqualPath workspace guidance

Before planning or implementing Iteration 2, read [`ITERATION1_HANDOVER.md`](ITERATION1_HANDOVER.md) and the source documents it links. Treat the checked-in code and latest verification evidence as authoritative if older design material conflicts with the handover.

Do not expose credentials or mutate/delete the existing live Appwrite owner data, send external handover requests, or change LeanKit cards unless the user explicitly authorises that scope.
